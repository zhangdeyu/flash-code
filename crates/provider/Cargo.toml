[package]
name = "flash-provider"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "flash_provider"
path = "src/lib.rs"

[dependencies]
flash-core = { path = "../core" }

[lints]
workspace = true
