[package]
name = "flash-tools"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "flash_tools"
path = "src/lib.rs"

[dependencies]
flash-core = { path = "../core" }

[lints]
workspace = true
