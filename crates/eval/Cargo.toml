[package]
name = "flash-eval"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "flash_eval"
path = "src/lib.rs"

[dependencies]
flash-agent = { path = "../agent" }
flash-core = { path = "../core" }
flash-tools = { path = "../tools" }

[lints]
workspace = true
