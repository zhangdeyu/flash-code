[package]
name = "flash-tui"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
name = "flash_tui"
path = "src/lib.rs"

[dependencies]
flash-core = { path = "../core" }

[lints]
workspace = true
